---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {%- assign pages = site.tabs | concat: site.pages -%}
  {%- assign seen_urls = '' | split: '' -%}
  {%- for page in pages -%}
    {%- if page.sitemap == false -%}
      {%- continue -%}
    {%- endif -%}
    {%- unless page.url == '/' or page.url contains '/topics/' or page.collection == 'tabs' -%}
      {%- continue -%}
    {%- endunless -%}
    {%- if seen_urls contains page.url -%}
      {%- continue -%}
    {%- endif -%}
    {%- assign seen_urls = seen_urls | push: page.url %}
  <url>
    <loc>{{ page.url | absolute_url }}</loc>
    <lastmod>{{ page.last_modified_at | default: site.time | date_to_xmlschema }}</lastmod>
  </url>
  {%- endfor -%}
  {%- for post in site.posts %}
  <url>
    <loc>{{ post.url | absolute_url }}</loc>
    <lastmod>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</lastmod>
  </url>
  {%- endfor %}
</urlset>
